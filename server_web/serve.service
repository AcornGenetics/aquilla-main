[Unit]
Description=Serve Astro dist via "serve"
After=network.target

[Service]
Type=simple
WorkingDirectory=/home/pi/app/
ExecStart=/usr/local/bin/serve dist
Restart=on-failure
Environment=NODE_ENV=production

[Install]
WantedBy=multi-user.target


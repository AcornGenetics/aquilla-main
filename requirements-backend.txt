fastapi
uvicorn[standard]
pydantic
requests
httpx
numpy
matplotlib
pyserial
RPi.GPIO
spidev
smbus2
cryptography
# AWS IoT Device SDK v2 — provides awsiot.greengrasscoreipc, which the on-device
# update agent (am#382) uses to talk to the Greengrass nucleus over Core IPC
# (publish the Device Shadow, defer component updates). Without it the agent's
# import fails and it silently no-ops — no shadow, no operator Update gate.
awsiotsdk
# boto3 — used by the profile-sync Greengrass component, which REUSES this api image
# (run with a different command + network_mode: host, ADR-022) to fetch profile
# bodies from S3 via the Token Exchange Role. The app process itself never calls S3;
# it only reads profiles/managed/. boto3 lives here so there's no second image.
boto3

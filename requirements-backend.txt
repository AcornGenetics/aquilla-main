fastapi
uvicorn[standard]
pydantic
requests
httpx
numpy
matplotlib
pyserial
RPi.GPIO
spidev
smbus2
cryptography
# AWS IoT Device SDK v2 — provides awsiot.greengrasscoreipc, which the on-device
# update agent (am#382) uses to talk to the Greengrass nucleus over Core IPC
# (publish the Device Shadow, defer component updates). Without it the agent's
# import fails and it silently no-ops — no shadow, no operator Update gate.
awsiotsdk
# (boto3 was here for the in-container profile fetch — removed in am#488: profile
# sync moved to the native com.acorn.profile-sync component per ADR-022, so the app
# image no longer fetches from S3. The component carries boto3 in its own venv.)

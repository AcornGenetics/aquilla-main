[Unit]
Description=Uvicorn application service
After=network.target

[Service]
User=pi
WorkingDirectory=/home/pi/aquila/aquila_web
ExecStart=/home/pi/aquila/bin/uvicorn main:app --host 0.0.0.0 --port 8090
Restart=on-failure

[Install]
WantedBy=multi-user.target

services:
  backend:
    image: ghcr.io/${GHCR_REPO:-acorngenetics/aquilla-main}-api:${IMAGE_TAG:-latest}
    labels:
      - "com.centurylinklabs.watchtower.enable=true"
    ports:
      - "8090:8090"
    environment:
      DEVICE_ID: "${DEVICE_ID}"
      RUN_MODE: "${RUN_MODE}"
      KIOSK_CONTROL_URL: "http://host.docker.internal:9191"
      DATA_DIR: "/opt/aquila"
      PROFILE_DIR: "/opt/aquila/profiles"
      RESULTS_PATH: "/opt/aquila/results/results.json"
    volumes:
      - ../data/results:/opt/aquila/results
      - ../data/logs:/opt/aquila/logs
      - ../data/profiles:/opt/aquila/profiles
      - ../data/config:/opt/aquila/config
    devices:
      - "/dev/ttyACM0:/dev/ttyACM0"
      - "/dev/i2c-1:/dev/i2c-1"
      - "/dev/spidev0.0:/dev/spidev0.0"
      - "/dev/spidev0.1:/dev/spidev0.1"
      - "/dev/gpiomem:/dev/gpiomem"
    privileged: true
    restart: unless-stopped

  ui:
    image: ghcr.io/${GHCR_REPO:-acorngenetics/aquilla-main}-ui:${IMAGE_TAG:-latest}
    labels:
      - "com.centurylinklabs.watchtower.enable=true"
    ports:
      - "8080:80"
    environment:
      DEVICE_ID: "${DEVICE_ID}"
      RUN_MODE: "${RUN_MODE}"
    restart: unless-stopped
